
CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

PROJECT(example01a)

FIND_PACKAGE(PkgConfig)
PKG_CHECK_MODULES(LIB REQUIRED gtk4)

INCLUDE_DIRECTORIES(${LIB_INCLUDE_DIRS})

ADD_EXECUTABLE(example01a
	main.c
)
TARGET_LINK_LIBRARIES(example01a ${LIB_LIBRARIES})

